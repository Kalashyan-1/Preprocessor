#include "define.h"

int main() {
        std::cout << "Please enter the name of translation unit: ";
        std::cin >> name;
        dir_name d;
        pars_1(tmp1, tmp2, include_line_num);
        dir_name_init(&d);
        if (tmp1 != "") {
        init_func_pointer(func_pointer);
        determin_dir_name(include_line_num, tmp1, &d,func_pointer);
        }
        define(&d);

}


